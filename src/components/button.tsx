"use client"
import { log } from "console";

 // Ensure this is a client-side component



interface ButtonProps {
  category: string; // Define the expected type for the category
}

export default function Button({ category }: ButtonProps) {
console.log(category);


  // Handle button click to navigate to a specific category page
  

  return (
    <>
     
    </>
  );
}
